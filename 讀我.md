### 第一次的 Prompt 如下

```
幫我把這個 Blog 做以下調整
- 更改為正體中文
- 把 theme 設定成 dark
- 名字是 Johnny Zhou
- 位置是 Taiwan, Kaohsiung
- 讀取 /resource 中的三個 pdf 讀檔，依內容幫我產生十篇樣板文章
```

由於產生的樣板文章和我上傳的檔名有完全的相關性，我以為真的有去讀取 pdf 的內容。

但檢查了一下生成的文章內容馬上就發現 AI 只讀取了檔案名稱而已。

除此之外的工作都有順利完成。

### 第二次的 Prompt 如下

```
目前 _post 中的十篇文章是我要求參考 /resource 中的三個 pdf 文件內容來撰寫的樣本文章，但文章內容看起來只參考了檔案名稱，請根據 pdf 文件的內容再重新編寫這十篇文章
```

其實我已經預期仍然無法讀取了，這次執行後我去看 Codex 那邊的記錄檔檢查 AI 的執行過程。

幾乎所有調用 pdf 工具的動作都是失敗的，這邊已經可以肯定要靠 Codex 去參考 pdf 檔這件事是行不通的。

### 第三次的 Prompt 如下

這次我先把本來 pdf 的內容都轉成 md 檔案上傳後才跑 Codex 執行下面這個 Promot

```
目前 _post 中的十篇文章是我要求參考 /resource 中的三個 pdf 文件內容來撰寫的樣本文章，但文章內容看起來只參考了檔案名稱，由於我判斷你無法讀取 pdf 檔案，請根據 /resource 目錄下的三個 md 文件的內容再重新編寫這十篇文章
```

的確就能成功讀取文章內容了，但我覺得改寫出來的文章品質蠻差的，目前推斷 Codex 的模型在文章撰寫的能力應該遠不如 ChatGPT 這邊的水準。

現階段還是得挑選適當的工具來處理對應的工作才能有比較好的成果。
